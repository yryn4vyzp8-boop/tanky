{
  "name": "@tanky/domain",
  "version": "0.1.0",
  "private": true,
  "description": "Shared domain layer for TANKY: transaction state machine, provider interfaces, mock providers, demo seed data",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:watch": "tsc -p tsconfig.json --watch",
    "test": "node --experimental-vm-modules --test --import tsx ./src/**/*.test.ts"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "tsx": "^4.19.2",
    "@types/node": "^22.9.0"
  }
}
